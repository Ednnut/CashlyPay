NODE_ENV=development
PORT=3000

# Square credentials
SQUARE_ACCESS_TOKEN=your-square-access-token
SQUARE_WEBHOOK_SIGNATURE_KEY=your-webhook-signature-key

# Admin token for securing management routes
ADMIN_API_TOKEN=long-random-token

# Optional: path to custom service catalog JSON file
# SERVICES_CONFIG_PATH=./config/services.custom.json
